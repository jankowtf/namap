valid__name_types <- function() {
  values <- c(
    "internal",
    "external",
    "external_clean"
  )
  # if (value_env_var <- Sys.getenv("R_CONFIG_NAMES_TYPE") != "") {
  #   value_env_var
  # } else {
  #   values
  # }
}
